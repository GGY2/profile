import React from 'react';
import { Card } from 'react-bootstrap';

const FullName = () => {
    return(
        <>
            <Card.Title>
                Alfred Nobel
            </Card.Title>
        </>
    );
};

export default FullName;